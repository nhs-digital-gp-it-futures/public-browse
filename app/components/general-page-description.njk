{% macro generalPageDescription(params) %}
  <div class="nhsuk-grid-row">
    <div class="nhsuk-grid-column-two-thirds">
      {% if params.titleText %}
        <h1 data-test-id="general-page-title" class="nhsuk-u-margin-top-3 nhsuk-u-margin-bottom-3">{{ params.titleText }}</h1>
      {% endif %}
      {% if params.descriptionText %}
        <div data-test-id="general-page-description">
          {% for text in params.descriptionText %}
          <div class="nhsuk-u-font-size-22 nhsuk-u-margin-bottom-2">{{ text }}</div>
          {% endfor %}
        </div>
      {% endif %}
    </div>
  </div>
{% endmacro %}
